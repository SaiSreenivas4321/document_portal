from setuptools import setup, find_packages

setup(
    name="documentPortal_LLMOPS",
    author="sai Sreenivas LLMops",
    version="0.1",
    description="A package for document portal LLM operations",
    packages=find_packages(),
    install_requires=[
        # List your package dependencies here
    ],
)